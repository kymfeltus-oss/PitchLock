# Local checklist for pitch sync + intelligence (run from repo root after configuring Supabase CLI).
# Uncomment and adapt paths to your environment.

# Invoke-Sql -File ./supabase/migrations/<your_latest_migration>.sql
# supabase db push
# supabase functions deploy track-engagement
# npm run build

Write-Host "See comments in finalize-pitch-engine.ps1 for deployment steps."
